# name: My Infracost Workflow

# on:
#   pull_request:
#     branches:
#       - main

# jobs:
#   infracost:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v2
#       with:
#         ref: ${{ github.event.pull_request.head.sha }}
#     - name: Run Terraform Plan
#       run: |
#         terraform plan -out=plan.out
#     - name: Run Infracost
#       run: |
#         infracost --tfplan plan.out
